apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: petclinic-dev
type: kubernetes.io/dockerconfigjson
data:
  # This will be updated by the Jenkins pipeline with actual credentials
  # Base64 encoded Docker config JSON for ghcr.io authentication
  .dockerconfigjson: REPLACE_ME_BASE64_DOCKERCONFIGJSON 